// scripts/set-admin.mjs
// Firebase Admin SDK removed in this static-export-friendly repository.
// To set admin claims, run a dedicated server-side script or use the
// Firebase CLI / Cloud Function with the Admin SDK. This file is left
// as a placeholder to document the previous workflow.

console.error('This script requires the Firebase Admin SDK and is disabled in the static export build.');
console.error('Re-enable by restoring firebase-admin imports or run an admin operation from a secure server environment.');
process.exit(1);
